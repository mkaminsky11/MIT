/*
var tesseract = require('node-tesseract');

tesseract.process(__dirname + '/output/test_1.png',function(err, text) {
    if(err) {
        console.error(err);
    } else {
        console.log(text);
    }
});
*/

var cv = require("opencv");